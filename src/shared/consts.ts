const constants = {
  BASE_URL: "https://jsonplaceholder.typicode.com",
};

const enum PathRoutes {
  posts = "/",
  userDetails = "/user-details",
  aboutMe = "/about-me",
}

export { constants, PathRoutes };
